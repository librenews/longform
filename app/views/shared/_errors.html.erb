<% if object.errors.any? %>
  <div style="background: var(--pico-del-color); color: white; padding: 1rem; border-radius: 0.5rem; margin-bottom: 2rem;">
    <h4 style="margin: 0 0 0.5rem 0; font-size: 1rem;">
      <%= pluralize(object.errors.count, "error") %> prohibited this post from being saved:
    </h4>
    <ul style="margin: 0; padding-left: 1.5rem;">
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
